#include <stdio.h>
#define N 7
typedef struct list
{
    int value;
    strcut list *next;
}List, *ListPtr;

void main()
{
    int A[N] = {329, 457, 657, 839, 436, 720, 355}
    ListPtr array[10];
    ListPtr data, p;
    p = data;
    p = (List *)malloc(sizeof(List));

    int i = 0;
    while (i < N)
    {
        data->next = p;

    }
}


